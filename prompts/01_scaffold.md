# Prompt 01 — Project Scaffold

**Context:** Read ENGRAM_BUILD_SPEC.md first.

**Task:**
Create the full project folder structure for ENGRAM exactly as described in the spec.
For every `.py` file, add:
1. The BSL 1.1 license header (from spec)
2. A module docstring explaining what the file does
3. Placeholder `pass` for all functions

Do NOT implement any logic yet — just structure.

**Output:** All files created with headers and empty function stubs.
